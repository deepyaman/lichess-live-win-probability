[project]
name = "lichess_live_win_probability"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9,<3.13"
dependencies = [
    "dagster",
    "dagster-cloud",
]

[project.optional-dependencies]
dev = [
    "dagster-webserver", 
    "pytest",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.dagster]
module_name = "lichess_live_win_probability.definitions"
project_name = "lichess_live_win_probability"

[tool.setuptools.packages.find]
exclude=["lichess_live_win_probability_tests"]
